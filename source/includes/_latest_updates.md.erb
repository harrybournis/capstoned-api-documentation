# Latest Updates

### Changelog (last updated: <%= Date.today %>)

### 1/5/17
- Added [GET /project-evaluations](<%= url %>#index-pending-project-evaluations-lecturer)
- Updated [POST /projects/:id/evaluations](<%= url %>#create-project-evaluation) to include multiple feelings with percent.
- Updated examples in [GET /assignments/:id/points](<%= url %>#index-points-for-assignment-lecturer) and [GET /projects/:id/points](<%= url %>#index-points-for-project-lecturer) to include rank, project_name, logo, color.

### 26/4/17
- Added [Form Templates](<%= url %>#form-templates)
- Added endpoint [POST /v1/assignments/:assignement_id/pa_forms](<%= url %>#create-pa-form-multiple)
- Added example for creating Assignment with [nested Iterations](<%= url %>#create-assignment)

### 21/4/17
- Changed create and update Project Evaluation routes to [POST /v1/projects/:project_id/evaluations](<%= url %>#create-project-evaluation) and [PATCH /v1/projects/:project_id/evaluations](<%= url %>#update-project-evaluation)
- Added ['pending_project_evaluation'](<%= url %>#index-projects-lecturer) field in Project to indicate if the current user can submit a project evaluation for this project.
- Removed statistics endpoints until they are refactored.
- Fixed bug where [GET /v1/pa_forms](<%= url %>#index-pa-forms) would return the currently active pa_forms(in terms of time), but also include forms already completed by the Student.
- Added endpoint [GET /v1/questions](<%= url %>#index-questions) for the Lecturer
- Added endpoint [GET /v1/reasons](<%= url %>#get-reasons)

### 18/4/17
- Added [Points](<%= url %>#points) resource.
- Added GET projects/:id/points, and GET /assignments/:id/points endpoints.
- Fixed [POST /pa_forms](<%= url %>#create-pa-form) example incorrectly showing ids were needed in the request.
- Added new [Game Settings](<%= url %>#create-game-settings) attribute max_logs_per_day
- Updated [Feelings](<%= url %>#feeling) description and examples.
- Updated [Confirm Account URL](<%= url %>#confirm-account)

### 23/3/17
- Added Game Settings (GET, POST, PATCH)
- Fixed bug in [logs](<%= url %>#create-log) that showed that the route was /update_logs instead of /logs

### 9/3/17
- Added GET /question_types
- Updated examples for the new questions format which contains question type

### 21/2/17
- Added GET /projects for lecturer that returns the active Projects
- Changed attributes for compact requests for units, assignments and projects. They now also include the name.

### 24/1/17
- Added unit_id to projects table
- Added [Get Projects for Unit ID](<%= url %>#get-projects-with-unit) endpoint
